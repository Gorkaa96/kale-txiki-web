import SimpleShell from "../simple-shell";

export const metadata = { title: "Política de cookies" };

export default function Page() {
  return <SimpleShell kicker="Cookies" title="Política de cookies"><p>Esta web tiene finalidad informativa. En esta versión no se utiliza un sistema propio de analítica ni publicidad.</p><p>Si más adelante se añaden nuevos servicios, esta página se actualizará.</p></SimpleShell>;
}
